name: roboex-vla-x3plus
# For ROS2 bridge/recorder: source /opt/ros/jazzy/setup.bash BEFORE activating this env.
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pyyaml
  - pip
  - pip:
    - hf_transfer
    - lerobot[smolvla]
